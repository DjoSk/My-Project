<Window x:Class="Atelier_des_Mots.Views.StudentSyllableExerciseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Syllable Exercise" Height="600" Width="800">

    <Window.Background>
        <ImageBrush ImageSource="D:/Project1/Atelier des Mots/Views/Resources/Images/Kid4.jpg" />
    </Window.Background>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Height="360">
                <WrapPanel Name="SyllableContainer" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <!-- Syllables will be added dynamically here -->
                </WrapPanel>
            </ScrollViewer>

            <Button Content="Assemble Word" Click="AssembleSyllable_Click" Width="300" FontSize="20" FontWeight="Bold" Background="LightGreen" Margin="0,0,0,20" />
            <Button Content="Next Word" Click="NextWord_Click" Width="300" FontSize="20" FontWeight="Bold" Background="LightBlue" Margin="0,0,0,20" />


            <Border Name="SyllableWordAssemblyDisplay" Background="LightYellow" BorderBrush="Black" BorderThickness="5" Width="980" Height="327" Margin="5">
                <TextBlock Name="SyllableAssembledWordText"
                           FontSize="100"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Text="{Binding AssembledWord}" />
            </Border>
        </StackPanel>
    </ScrollViewer>
</Window>
