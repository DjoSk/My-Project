<Window x:Class="Atelier_des_Mots.Views.StudentSyllableExerciseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exercice sur les mots syllabiques" Height="600" Width="800">

    <!-- Background Image -->
    <Window.Background>
        <ImageBrush ImageSource="pack://siteoforigin:,,,/Views/Resources/Images/Kid1.jpg" Stretch="UniformToFill" />
    </Window.Background>

    <Grid>
        <!-- MediaElement for Background Music -->
        <MediaElement x:Name="BackgroundMusic"
                      Source="pack://application:,,,/Atelier_des_Mots;component/Views/Resources/Sounds/Music.mp3"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Stop"
                      Visibility="Collapsed" />

        <!-- Main Content -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Height="350">
                    <WrapPanel Name="SyllableContainer" HorizontalAlignment="Center" Margin="0,0,0,20">
                        <!-- Syllables will be added dynamically here -->
                    </WrapPanel>
                </ScrollViewer>

                <Button Content="Construis le mot magique !" Click="AssembleSyllable_Click" Width="550" FontSize="30" FontWeight="Bold" Background="LightGreen" Margin="0,0,0,20" />
                <Button Content="Avance vers un autre mot magique !" Click="NextWord_Click" Width="550" FontSize="30" FontWeight="Bold" Background="LightBlue" Margin="0,0,0,20" />

                <Border Name="SyllableWordAssemblyDisplay" 
                 Background="LightYellow" 
                BorderBrush="Black" 
                BorderThickness="5" 
                Width="1100" 
                Height="230" 
                Margin="5" 
                Visibility="Collapsed">
                            <TextBlock Name="SyllableAssembledWordText"
                       FontSize="110"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Text="{Binding AssembledWord}"
                       FontFamily="Arial"/>
                    
                        </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
