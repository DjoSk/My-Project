<Window x:Class="Atelier_des_Mots.Views.StudentExerciseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Student Exercise" Height="600" Width="800">

    <Window.Background>
        <ImageBrush ImageSource="Resources/Images/BackgroundWordsFlying.jpg" />
    </Window.Background>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
            <!-- Syllables Area -->
            <WrapPanel Name="SyllableContainer" HorizontalAlignment="Center" Margin="0,0,0,20" />

            <!-- Selected Syllable Display Area -->
            <Border Name="SelectedSyllableDisplay" Background="LightCoral" BorderBrush="Black" BorderThickness="2" Width="400" Height="80" Margin="0,0,0,20">
                <TextBlock Name="SelectedSyllableText" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" />
            </Border>

            <!-- Assemble Button -->
            <Button Content="Assemble Word" Click="AssembleWord_Click" Width="200" FontSize="16" FontWeight="Bold" Background="LightGreen" Margin="0,0,0,20" />

            <!-- Full Word Display Area -->
            <Border Name="WordAssemblyDisplay" Background="LightYellow" BorderBrush="Black" BorderThickness="2" Width="500" Height="100" Margin="0">
                <TextBlock Name="AssembledWordText" FontSize="28" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" />
            </Border>
        </StackPanel>
    </Grid>
</Window>
